# AssetLens AI R14.2 — Correct Database-backed Offline QR

## المشكلة التي تم إصلاحها

كان QR يُنشأ من صف التقارير الموجود في ذاكرة المتصفح. إذا كان هذا الصف قديمًا أو ناقصًا، كان الرمز يُطبع بدون بيانات الأصل الحقيقية.

## الحل

- endpoint جديد: `GET /api/assets/:id/qr`.
- يتحقق من تسجيل الدخول والصلاحية، ثم يقرأ الأصل نفسه من Supabase بالـID.
- يقرأ حقول تحليل AI من `assets.fields`.
- يقرأ جميع القيم من `asset_custom_values` وتسميات الحقول من `custom_fields`.
- يبني Snapshot ذاتية الاكتفاء داخل QR، تشمل بيانات المشروع والموقع والحالة وGPS والتواريخ والحقول.
- يحافظ على `0` و`false` كقيم صحيحة ولا يحولها إلى فراغ.
- يرفض إنشاء رمز لسجل فارغ أو غير موجود بدل طباعة QR مضلل.
- يستخدم payload مضغوطة وأصغر (`alqr2`) مع توافق رجعي لرموز `alqr1`.
- رُفع حجم QR الظاهر في ملصق الطباعة من 180px إلى 260px فقط، مع مصدر 840px وحافة بيضاء أكبر لتحسين المسح بدون تضخيم الملصق.
- أصبح QR الافتراضي نصيًا مباشرًا: أي قارئ QR عادي يعرض بيانات الأصل نفسها بدون رابط أو نشر أو إنترنت، مع بقاء قراءة رموز `/scan#alqr2` السابقة مدعومة.

## الاختبارات

- QR encode/decode roundtrip حقيقي.
- اختبار Asset ID.
- اختبار AI fields وcustom fields.
- اختبار القيم الصفرية.
- اختبار رفض السجل الفارغ.
- ESLint وTypeScript وNext.js production build وجميع اختبارات الجودة.

لا يحتاج هذا الإصلاح إلى SQL إضافي؛ يلزم رفع ملفات R14.2 إلى GitHub ليعيد Vercel النشر.
