# AssetLens AI R13.1 — ملاحظات الإصلاح

## الأخطاء التي عالجتها هذه النسخة

1. **فشل Gemini بسبب `gemini-2.5-flash`**
   - لم يعد المشروع يستخدم هذا الموديل كـ fallback.
   - إذا كانت قيمة `GEMINI_MODEL` القديمة ما زالت 1.x أو 2.x، يتجاوزها النظام تلقائيًا.
   - الترتيب الافتراضي الآن: Gemini 3.7 Flash ثم 3.6 Flash ثم 3.5 Flash.
   - إذا تغيرت أسماء الموديلات مستقبلًا، يستخدم النظام `models.list` لاكتشاف موديل Flash/Pro متوافق مع المفتاح.

2. **ظهور قائمتين جانبيتين وتزاحم Dashboard**
   - كان Service Worker القديم قادرًا على تخزين Next.js RSC قديم ودمجه مع Layout جديد.
   - تم تغيير إصدار الكاش، حذف كاش AssetLens السابق، ومنع تخزين RSC وPrefetch نهائيًا.
   - تم تسجيل Service Worker من الـApp Shell لكل الصفحات مع تحديث إجباري آمن وإعادة تحميل مرة واحدة عند انتقال الإصدار.
   - أضيفت حماية CSS تمنع ظهور Sidebar قديم حتى أثناء لحظة تحديث الكاش.

3. **عرض رسالة خطأ التحليل**
   - أصبحت رسالة المزود تلتف داخل السطر ولا تُقص أو توسع الصفحة أفقيًا.

## خطوات التحديث

1. ارفع مشروع R13.1 كاملًا بدل نسخ ملفات منفردة فوق مشروع قديم.
2. يفضل ضبط متغير Vercel التالي، رغم أن النظام يتجاوز القيم القديمة تلقائيًا:

   ```env
   GEMINI_MODEL=gemini-3.7-flash
   ```

3. نفّذ Redeploy ثم افتح الموقع. قد يعيد التطبيق تحميل الصفحة مرة واحدة لحذف كاش الإصدار القديم.
4. افتح Capture & Analyze واضغط **إعادة المحاولة** للمهمة الفاشلة.

## قاعدة البيانات

لا توجد migration جديدة في R13.1. إذا سبق أن نفذت `setup.sql` وملفات migrations حتى `005_dashboard_snapshot.sql` فلا تعد تشغيلها.
