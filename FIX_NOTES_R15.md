# AssetLens AI R15 — Product Upgrade

## قاعدة البيانات

شغّل `supabase/migrations/008_global_product_upgrade.sql` مرة واحدة في Supabase SQL Editor بعد الترقية السابقة 007.

> تحديث 4 سبتمبر 2026: تم تصحيح اسم عمود سلسلة الأيام داخل دالة لوحة التحكم لتفادي خطأ PostgreSQL `syntax error at or near "day"`. يمكن إعادة تشغيل الملف كاملًا بأمان؛ أوامر الترقية قابلة لإعادة التنفيذ.

> تحديث إضافي: تم نقل حساب أعلى عدد للأصول في المشاريع إلى CTE مستقل باسم `project_stats`، حتى لا تحتوي دالة التجميع `jsonb_agg` على دالة نافذة، وهو غير مسموح في PostgreSQL.

> إصلاح تشغيل محلي: تم منع Service Worker في وضع التطوير ومسح كاش AssetLens القديم مرة واحدة قبل إعادة التحميل. هذا يمنع خلط قائمة تنقل قديمة مع HTML الجديد، وهو سبب خطأ React Hydration الظاهر في سجل `npm run dev`. كما تم تصحيح نسبة أبعاد الشعار وتعريف سلوك التمرير لـNext.js.

الملف آمن لإعادة التشغيل ويضيف:

- ربط الحقول المخصصة بأنواع أصول محددة.
- وحدة القياس، الاستخراج بالذكاء الاصطناعي، والظهور في التقارير وQR.
- دالة Dashboard مفلترة تحترم RLS وصلاحيات المستخدم.
- فهرس مركب لفلاتر المشروع والموقع والتاريخ.

## الوظائف الجديدة

- مسار التقاط من 3 خطوات مع اختيار مستقل للكاميرا أو معرض الصور على الويب بالموبايل.
- إضافة أصل يدويًا بدون صورة.
- حقول إلزامية أو اختيارية حسب نوع الأصل، مع تعبئة تلقائية اختيارية من AI.
- فلاتر Dashboard إدارية حسب المشروع والمبنى والطابق والزون.
- دمج Asset Register داخل Reports مع العرض والنقل والحذف.
- QR Offline أصغر ويُبنى من نسخة حديثة من سجل Supabase بالـAsset ID.
- شاشة تحميل متحركة بشعار النظام وخط Cairo/System Fonts.

## الصلاحيات

- إنشاء المستخدمين وإرسال الدعوات وتحديد الدور والمشاريع متاح فقط للسوبر أدمن `eng.ahmedsalman96@gmail.com`.
- الدور `Admin` يرى كل المشاريع، والدور `Surveyor` يرى فقط المشاريع المسندة إليه.
- لا يوجد تسجيل ذاتي من صفحة الدخول.

## التحقق

- ESLint: ناجح.
- TypeScript: ناجح.
- Next.js production build: ناجح.
- اختبارات الجودة: 22 اختبارًا ناجحًا.
