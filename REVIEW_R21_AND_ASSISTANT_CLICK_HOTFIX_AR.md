# مراجعة AssetLens AI R21 وإصلاح نقر المساعد العائم

**النطاق:** ملف `AssetLens_AI_R21_Professional(1).zip` المرفق وسجل ملاحظات 12 سبتمبر 2026 والصورة التي تُظهر زر المساعد في صفحة التقرير. هذه حزمة إصلاح صغيرة: `app/assistant.css` لإصلاح النقر والطباعة، و`app/components/app-shell.tsx` و`public/sw.js` لتجديد نسخة الواجهة المحفوظة محليًا إلى `21.0.1`؛ ولا تتضمن تعديلات R22 المذكورة في السجل. لم تُفحص قاعدة Supabase أو جلسة المستخدم المنشورة أو ملف Excel الحقيقي.

## العطل الذي عولج في هذه النسخة المحدودة

في R21، عنصر `.assistant-dock` ثابت في أسفل يمين الشاشة وذو `z-index:1200`، وبداخله `.assistant-panel` مخفي بـ`visibility:hidden` و`pointer-events:none`، لكنه يظل مشاركًا في تحديد مساحة العنصر الأب. كان العنصر الأب نفسه يستقبل النقر داخل المستطيل الكبير فوق واجهة الصفحة؛ هذا يفسر ضياع النقرات المقصودة لبعض الأزرار والحقول، بما فيها ناحية زر حفظ/طباعة التقرير في الصورة، ويجعل تفسير المشكلة بغياب `onClick` في التبويبات غير كافٍ.

أضيف `pointer-events:none` إلى `.assistant-dock`، مع إعادة `pointer-events:auto` صراحةً لزر المساعد وفقاعة الترحيب المرئيين، وإخفائه عند الطباعة. تبقى نافذة المحادثة عند فتحها قابلة للتفاعل بقاعدة `.assistant-dock.is-open .assistant-panel{pointer-events:auto}` الموجودة أصلًا. هذا **إصلاح للشيفرة، لا تأكيد لتجربته في حساب المستخدم**. يلزم اختبار نقر زر التقرير، حواف التبويبات، اختيار المشروع، خانة اسم المبنى، وأزرار الحفظ على سطح المكتب والهاتف، في الثيمين وبالماوس واللمس ولوحة المفاتيح. الزر المرئي للمساعد نفسه قد يظل يغطي جزءًا مما يقع تحته مباشرة في شاشات صغيرة؛ إن حدث ذلك يلزم تعديل موضعه أو إزاحة إجراءات الصفحة، لا تعطيل النقر على الزر.

## نتيجة دراسة الملاحظات على R21

| المجال | الموجود في R21 | الفجوة أو الحكم | شواهد الشيفرة |
| --- | --- | --- | --- |
| التنقل ومساحات النقر | تبويبات التوأم/العلاقات/الميزانية أزرار تستدعي `setTab`. يوجد تنسيق للهاتف والداكن والتركيز في مواضع. | اعتراض طبقة المساعد سبب مؤكد بالشيفرة ومحتمل بقوة للصورة؛ أُصلح موضعيًا. لا يوجد إثبات أن **كل** عناصر التطبيق قابلة للنقر من جميع الحواف؛ يلزم اختبار تفاعلي. | `app/intelligence/page.tsx`؛ `app/assistant.css`؛ `app/globals.css` |
| سؤال إجمالي الأصول | المساعد يستقبل أصول المشروع المسموح بها ويملك تحليلًا عبر Gemini/OpenAI عند توافر مفتاح خادم. | دالة البحث الاحتياطي تعالج كلمات مثل `my` و`project` و`مشروعي` ككلمات بحث في الأصل؛ إعادة إنتاج محلية على 36 أصلًا مصطنعًا أعطت 0 بالإنجليزية والعربية. تحميل المساعد يستبعد غير `review/completed`، فلا يطابق دائمًا «كل الأصول» في التقارير. | `app/lib/asset-intelligence.ts`، `app/lib/server/ask-assetlens.ts`، `app/api/assistant/route.ts`، `app/api/intelligence/route.ts` |
| اختيار المشروع | GET يرجع معرّف أول مشروع مسموح به إذا غاب المحدد؛ POST يفحص صيغة UUID ووصول المشروع. | لا توجد إعادة إنتاج موثقة لرسالة `Choose a valid project` على حساب MOEI. عند الطلبات المتزامنة قد تصل استجابة `load()` القديمة بعد تغيير المشروع وتعيد التحديد القديم؛ يحتاج اختبار وتتبع القيم الفعلية. | `app/intelligence/page.tsx`، `app/api/intelligence/route.ts` |
| استيراد أكثر من 2000 أصل | قارئ XLSX يمر على جميع الأوراق ويكشف صف العناوين؛ CSV/TSV مدعومان؛ 200 صف لكل طلب؛ بصمة تمنع تكرار الطلب نفسه؛ صفوف ناقصة تحفظ للمراجعة. | المعاينة تعرض أول **5** صفوف فقط؛ لا يوجد سجل سابق لكل صف يحصي المقبول/المتجاهل/السبب. لا توجد وظيفة استيراد دائمة تستأنف من دفعة محفوظة بعد إغلاق المتصفح. كل دفعة تعيد جلب سيريالات المشروع. البصمة تشمل بيانات الصف والمبنى، وبالتالي تصحيح مبنى ثم إعادة الاستيراد قد يخلق أصلًا جديدًا بدل تحديث نفس الصف. اختبارات البيانات المصطنعة لا تثبت سبب توقف الملف الفعلي عند نحو 115 صفًا. | `app/lib/excel-client.ts`، `app/reports/page.tsx`، `app/api/reports/route.ts` |
| المبنى والحقول الناقصة | خانة مبنى عامة وتصحيح لكل صف قبل الرفع؛ الحالات والتقييمات الناقصة تبقى للمراجعة. | بناء واجهة إدخال لكل الصفوف دفعة واحدة قد يكون ثقيلًا مع آلاف الصفوف. يجب فحص الإكمال لاحقًا والتمييز بين صف فارغ جرى تجاهله وصف مرفوض في كشف شامل. | `app/reports/page.tsx`، `app/lib/import-normalization.ts` |
| استيراد المال والعمر والعلاقات | نموذج الأصل وقاعدة البيانات والتصدير يعرفان التكلفة والعمر والعملة، والعلاقات تُضاف من صفحة ذكاء الأصول. | تعيين الاستيراد لا يشمل `replacement_cost` أو `price_currency` أو `useful_life_years` أو `remaining_life_years`، ولا يوجد تعيين واضح لعلاقات الأصول. تحفظ الأعمدة غير المعروفة كنص مصدر فقط، فلا تستفيد المحاكاة من قيمها. | `app/reports/page.tsx`، `app/api/reports/route.ts`، `app/lib/asset-intelligence.ts` |
| الحالة التشغيلية | القيم الحالية تتضمن `unknown` ومطبّع الاستيراد يحول النصوص غير المعروفة إلى `unknown`. | ترحيل `013` يمنع `unknown`، بينما `017` يوسع القيد. إذا لم يُطبق `017` على قاعدة المستخدم فخطأ `assets_operational_status_check` متوقع؛ لا يوجد دليل هنا أنه طُبق أو أن البيانات القديمة صالحة عند إضافته. | `app/lib/asset-operational-status.ts`، `supabase/migrations/013_asset_management_categories_retention.sql`، `supabase/migrations/017_assistant_access_and_incomplete_imports.sql` |
| الهيكل المكاني والإدارة | إضافة المبنى والطابق والزون يدويًا؛ مستويات إضافية مطلوبة/اختيارية وعلاقات أب، وقيمها في الالتقاط والتصدير والاستيراد عند تطابق اسم العمود. بطاقات الفئات في نموذج المشروع وإدارة عامة للفئات. | المستوى الرابع والخامس وأكثر لم يُختبر هنا بقاعدة حقيقية. يلزم عرض سبب وجود محدد الفئات في المشروع وإدارة الفئات العامة بوضوح، وتعيين أعمدة المستويات الإضافية يدويًا عند اختلاف رؤوس الملفات. | `app/admin/page.tsx`، `app/api/config/route.ts`، `supabase/migrations/016_harden_qr_and_hierarchy.sql` |
| المحاكاة والتوأم | تجميع الأصول حسب المبنى والموقع، علاقة اعتماد وأثرها، ومحاكاة ميزانية من الأصول ذات التكلفة والحالة والأهمية المسجلة. | القيمة التقديرية تُستخدم بديلًا تلقائيًا لتكلفة الاستبدال، وتعرض المحاكاة كل الأسعار بـAED دون فحص عملة الأصل؛ هذا قد يعطي مجموعًا ماليًا مضللًا. الأصل ناقص التقييم يأخذ درجة مخاطرة 0 بدل «غير معروف». لا يظهر مدى تغطية بيانات التكلفة والتقييم داخل المحاكاة. | `app/api/intelligence/route.ts`، `app/lib/asset-intelligence.ts`، `app/intelligence/page.tsx` |
| «أصلحه أم أستبدله؟» | نموذج يطلب تكلفة إصلاح واختياريًا تكلفة استبدال ويحسب الفارق والنسبة بعملة الأصل. | لا يستخدم حالة الأصل وأهميته وعدد الأصول التابعة له لاقتراح قرار معلل؛ يحيل إلى مراجعة الأعطال والضمان. لا يجوز تقديم هذا الفرق الحسابي كتوصية هندسية نهائية. | `app/api/assistant/route.ts`، `app/lib/repair-decision.ts` |
| ربط الأصول وحذف المحدد | تخزين العلاقات والتحقق من أن طرفي العلاقة داخل المشروع؛ حذف أصل منفرد مع صلاحية. | لا سؤال اختياري للأب/الابن أثناء إضافة الأصل، ولا قصر خيارات الرابط على المبنى نفسه، ولا تدفق استيراد للعلاقات. لا يوجد زر أو API لحذف مجموعة مع فحص كل أصل وحد الدفعة وتأكيد واعٍ. | `app/intelligence/page.tsx`، `app/api/intelligence/route.ts`، `app/api/assets/route.ts`، `app/reports/page.tsx` |
| المساعد العائم والصلاحيات | مساعد عائم، ترحيب، محادثة، دعم reduced motion، فصل صلاحية المساعد وتقارير AI على الخادم والواجهة. تستخدم إجابات AI مفتاحًا من بيئة الخادم. | لا يوجد إعداد إداري لمفتاح Gemini في الواجهة. قد تتكرر دلالة «بحث وليس AI» في الوسم والنص التوضيحي. صفحة تقارير AI ما زالت تسمح بإرسال مفتاح محفوظ في `sessionStorage` بدل الاقتصار على إدارة خادمية. | `app/components/assetlens-assistant.tsx`، `app/api/assistant/route.ts`، `app/api/reports/ai/route.ts`، `app/reports/ai/page.tsx`، `supabase/migrations/017_assistant_access_and_incomplete_imports.sql` |
| التقارير وQR | تقرير مفصل مع ملاحظات اختيارية منفصلة عن الحقائق، وحجب توفير الكهرباء المالي غير المسند؛ QR يحمل بيانات أساسية داخل الملصق ويجلب أحدث البيانات أونلاين مع تخزين محلي. | لا يوجد اختبار طابعة/ماسح حقيقي. QR يطلب بيانات كل أصل ويوقف العملية عند فشل أي طلب؛ يلزم تجربة مهلة وإعادة محاولة ورسائل أخطاء قابلة للتصرف. تقارير AI لا تعادل تقريرًا مراجَعًا هندسيًا. | `app/reports/page.tsx`، `app/api/reports/ai/route.ts`، `app/lib/asset-qr.ts`، `app/scan/page.tsx` |
| الحسابات والأخطاء والتنبيهات | تسجيل الدخول يزيل جلسة سابقة قبل الدخول ويفصل التحديث المتأخر، وتغيير كلمة المرور وإدارة المستخدمين موجودة. | يلزم اختبار حسابين فعليين وتبديل الجلسة وصلاحيات المشروع وقاعدة Supabase؛ العديد من رسائل API بالإنجليزية. لا توجد آلية عامة تنقل المستخدم إلى أعلى الصفحة عند نجاح/فشل عملية؛ تحريك محادثة المساعد فقط لا يحقق هذا الطلب. | `app/lib/supabase-auth.ts`، `app/login/page.tsx`، `app/api/config/route.ts`، `app/components/app-shell.tsx` |

## ترتيب التنفيذ المقترح بعد هذا الإصلاح

1. **حاجز التشغيل:** اختبار تفاعلي للطبقة والنقر والتبويبات والمحددات في أحجام شاشات وثيمين مختلفين، وفحص DOM بـ`elementFromPoint` عند أطراف الأزرار والحقول. بعدها تثبيت العد الكامل في المساعد وتعريف حالات الأصول المشمولة وعزل كل مستخدم/مشروع، ثم علاج تزامن اختيار المشروع.
2. **سلامة البيانات:** فحص حالة ترحيل `017` على نسخة اختبار من Supabase، وتجربة ملف Excel الأصلي مع كشف لكل ورقة/صف، وتصميم جلسة استيراد قابلة للاستئناف وبصمة ثابتة للصف المصدر، وربط المال والعمر والعملة والتصنيف والمستويات والعلاقات بحقول أصل صريحة قبل ترحيل بيانات فعلية.
3. **قرار الأصل:** محاكاة لا تجمع عملات مختلفة ولا تستبدل التكلفة المقدرة بتكلفة استبدال موثقة، وتعرض الأصول المستبعدة بسبب نقص البيانات. توسيع مقارنة الإصلاح لتشمل الحالة والأهمية والعلاقات المسندة بوضوح.
4. **تشغيل المنتج:** حذف جماعي بتأكيد وصلاحيات لكل عنصر، تمرير الأخطاء/النجاح إلى أعلى الصفحة عند العملية المهمة فقط، تحسين QR وإعادة المحاولة، وفحص تباين الداكن وسلوك المحمول وجلسات الحسابات.

## التحقق وحدوده

نجح `npm test` بعد كل التعديلات: ESLint وTypeScript وبناء الإنتاج و**57** اختبارًا آليًا. اختبار مستقل محلي بـ36 أصلًا مصطنعًا أعاد **0** للاستفسارين `how many total asset in my project?` و`كم عدد الأصول في مشروعي؟`؛ هذا يعيد إنتاج خطأ دالة البحث الاحتياطي فقط، وليس نتيجة قاعدة المستخدم. **لم ينفذ فحص متصفح فعلي أو SQL أو استيراد ملف المستخدم في هذه المراجعة.**

لإغلاق العطل على النسخة المنشورة: تطبيق الترحيلات الصحيحة على قاعدة اختبار، ثم اختبار حساب مدير وحساب محدود في مشروع MOEI، ملف Excel الحقيقي (>2000 صف)، تقرير فعلي وQR مطبوع وممسوح، مع مطابقة إجمالي المشروع وعدد المدرج/المرفوض تفصيليًا.
