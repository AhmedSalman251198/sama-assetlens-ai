# AssetLens AI R20 — تقرير الاختبار والتسليم

## نتيجة الفحوص المحلية

- `npm test`: فحوص ESLint، ثم TypeScript، ثم بناء Next.js الإنتاجي، ثم اختبارات Node الآلية. تحقّق من نتيجة آخر تشغيل قبل النشر؛ حزمة الاختبار تحتوي 51 حالة.
- اختبار الاستيراد XLSX المولد محليًا يتأكد من 2,237 أصلًا على ورقتين مختلفتي اللغة وترتيب الأعمدة، ومن أول وآخر صف بكل ورقة، مع تحديد صف عناوين مختلف يدويًا.
- اختبار CSV المولد محليًا يتأكد من 2,407 أصلًا مع مقدمات وحقول مقتبسة؛ اختبار API إنتاجي مع Supabase **وهمي** يتحقق من إدخال 2,013 صفًا بدفعات، إعادة المحاولة دون مضاعفة، وصف مرفوض بسبب بياناته، ومنع دفعة فوق 250 صفًا.
- اختبار المساعد باستخدام Gemini وOpenAI **مقلّدين**: فلترة بمبنى وحالة وأهمية حقيقية، إحصاء السجل المطابق، رفض الاقتباسات من أصول خارج العينة، وتمييز بحث السجل الاحتياطي عندما يغيب المفتاح أو يتعطل المزود.
- اختبار تبديل الحسابات: إذا أخفق تحديث جلسة مستخدم سابق بعد نجاح دخول الأدمن، لا تمسح عملية الإخفاق جلسة الأدمن الجديدة.
- اختبارات QR تتناول محتوى الملصق الثابت وتحديث القراءة عبر التطبيق والحد الأدنى من البيانات العامة؛ اختبارات الهيكل والصلاحيات وتبعيات دورية والمحاكاة الحالية باقية.

## ماذا تغيّر في الاستخدام؟

1. عند اختيار ملف استيراد، راجع عدد الصفوف على كل ورقة ومعاينة الأعمدة. لو لم يلتقط صف العنوان صحيحًا، حدّد الرقم من «مراجعة أوراق الاستيراد» واضغط «تطبيق»؛ لا يبدأ الاستيراد قبل تطبيق التغيير.
2. عند إتمام الملف، يجب أن يساوي مجموع الصفوف المستوردة والمكررة والمرفوضة عدد الصفوف المقروءة. لو حدث انقطاع، يحتفظ النموذج بالملف للمحاولة؛ إعادة المحاولة لا تُضاعف الصفوف التي سجلها النظام.
3. في «اسأل AssetLens» تحتاج تشغيل `GEMINI_API_KEY` أو `OPENAI_API_KEY` على الخادم. تعرض البطاقة إن كانت «تحليل AI» أو «بحث السجل». كل تقدير أو اقتراح يستلزم مراجعة بشرية، خاصة أي قرار مالي أو سلامة مهنية.
4. انسخ آخر ZIP كاملًا إلى مشروع تجريبي أو فرع Staging، ثم نفذ migrations حتى `016` بالترتيب. لا تنفذها مباشرة على قاعدة الإنتاج دون نسخة احتياطية ومراجعة صلاحيات.

## لم يُثبت بعد على بيئة المستخدم

لم يتوفر ملف المستخدم الأصلي ذو 2,000+ صف، ولا بيانات اعتماد Supabase/Gemini على Staging؛ لذلك **لم** أختبر الاستيراد الحقيقي كاملًا، أو `015` و`016` على قاعدة المستخدم، أو إنشاء حساب/تغيير كلمة مرور بحسابات حقيقية، أو Gemini متصلًا بمصدره، أو Cron/Supabase Storage، أو QR مطبوعًا على هاتف، أو المظهر الليلي/الترجمة بفحص بصري على هواتف ومتصفحات فعلية. فحص التباين في CSS تحسّن، لكنه لا يساوي اختبار قابلية الوصول الشامل.

التوأم الرقمي الموجود سجل مكاني للأصول وليس نموذج BIM/3D أو تدفق حساسات مباشر. محاكاة الاستبدال تُرتب المخاطر ضمن الميزانية المتاحة دون حساب كل تغير مستقبلي للتكلفة أو الأعطال. لا يصح اعتبار مراجع ISO شهادة امتثال، ولا افتراضات التقرير عن الطاقة قياسات مثبتة.

## قبول المنتج قبل الإطلاق العام

- مطابقة عدد صفوف ملف العميل الفعلي: «المستورد + المكرر + المرفوض = جميع صفوف البيانات» في كل ورقة، وفحص عينات أول/وسط/آخر الملف يدويًا.
- تجربة الصلاحيات من المتصفح وطلب API مباشر لكل دور؛ واختبار الدخول والخروج ثم الدخول بحساب مختلف.
- تصوير اسم اللوحة والتصوير من المعرض، إدخال الأصل اليدوي والجماعي، تبرير الحالة، الاعتماد، نقل الأصل، QR متصل/منقطع؛ تجربة تغير حالة أصل بعد طباعة الرمز.
- مراجعة النسخة العربية/الإنجليزية والوضع النهاري/الليلي على جهازين على الأقل وحجوم شاشة مختلفة.
- اختبار تشغيل Cron بعد انقضاء مدة الصورة في بيئة تجريبية والتحقق من بقاء بيانات الأصل بعد الحذف.
