# AssetLens AI R19 — تقرير الجودة

## نتائج محلية

- `npm run lint`: ناجح بلا أخطاء أو تحذيرات.
- `npm run typecheck`: ناجح.
- `npm run build`: ناجح؛ شمل مسارات الالتقاط والإدارة والتقارير وAsset Intelligence وQR.
- `npm run test:quality`: 43 اختبارًا. راجع نتيجة آخر تشغيل محلي قبل النشر.
- اختبار API تكاملي على نسخة Next الإنتاجية مع خادم بيانات وهمي: أدخل 2,013 صفًا في دفعات حجمها 200؛ إعادة إرسال أول 200 صف لم تنشئ أصلًا مكررًا. الصف ذو التصنيف غير الصحيح يظهر ضمن المرفوضات، والدفعة الأكبر من 250 صفًا تُرفض قبل الحفظ.
- اختبار قراءة CSV فيه 2,407 صفوف وعنوان مسبق؛ طابق السطر 116 وآخر سطر، وجرّب الفاصلة المنقوطة والخلايا المقتبسة. اختبار XLSX متعدد الأوراق والعناوين العربية/الإنجليزية، وفحص pagination بعد 1,000 سجل، وفحص فشل واضح عند تجاوز حد 100,000 صف.
- اختبارات سلوك لـQR المباشر دون إنترنت، سرية الـQR العام، ترابط المستويات والمكاتب، حدود صلاحيات الصفحة، تبعيات دائرية ومحاكاة الميزانية.

## ما لم يُختبر على بيئة الإنتاج

لا توجد مفاتيح لمشروع Supabase الحقيقي أو مصدر AI ضمن هذه النسخة. لذا لم أُنفذ migrations `015` و`016` على قاعدة فعلية، ولم أختبر RLS أو إنشاء مستخدم حقيقي أو حذف ملفات Storage أو بحث AI متصلًا أو كاميرا هاتف وQR مطبوعًا أو تباين الواجهة بصريًا على أجهزة مختلفة. اختبار API أعلاه استخدم بيانات وهمية متعمدة؛ لا يحل محل اختبارات قبول الإنتاج.

## قبول النشر

1. خذ نسخة احتياطية ونفذ migrations بالترتيب حتى `016` في Staging، ثم افحص التطبيق بصلاحيات Admin وReviewer وViewer ومن خلال REST مباشر.
2. ارفع نسخة من ملفك الفعلي (2,000+ أصل)، وتأكد أن **المستورد + المكرر المتجاوز + المرفوض = عدد صفوف البيانات القابلة للاستيراد**؛ راجع التقرير لكل صف وجرّب إعادة الرفع.
3. امسح ملصق QR متصلًا وغير متصل باستخدام ماسح AssetLens وتحقق من الحالة بعد اعتمادها؛ قارئ الهاتف النصي يعرض لقطة الملصق الثابت فقط.
4. راجع الألوان والترجمات على الهواتف، ثم تحقق من تشغيل Cron وحذف ملف Nameplate فقط بعد 30 يومًا مع بقاء حقول الأصل.

راجع `VERCEL_DEPLOYMENT.md` لقائمة قبول أكثر تفصيلًا. لا تعتبر إشارات ISO شهادة اعتماد، ولا تعتبر الوفورات التقديرية قياسات فعلية.
