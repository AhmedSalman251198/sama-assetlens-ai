# AssetLens AI R14 — ملاحظات الإصلاح

## المشكلة الأصلية

كانت لوحة التحكم تؤدي دور عدة صفحات في الوقت نفسه، كما ظل جزء من الواجهة القديمة قابلًا للظهور من Cache الـPWA. هذا جعل أزرار القائمة تبدو بلا فائدة، زاد حجم شجرة React، وسبب تكرار الشريط الجانبي في بعض الجلسات. كذلك كانت عدة صفحات تعيد نفس طلبات Supabase، وكانت عملية رفع عدة صور تعيد كامل مساحة العمل بعد كل صورة.

## ما تم إصلاحه

- أصبحت `/` لوحة تحليلات وشارتات فقط، بلا Capture أو Asset Register أو Organization أو Transfer داخلها.
- كل زر في القائمة يستخدم مسار Next.js مستقلًا: `/capture`، `/assets`، `/organization`، `/locations`، `/transfers`، `/reports` و`/admin`.
- أزيلت الوحدات القديمة من JSX نهائيًا بدل إخفائها بـCSS.
- أضيف مؤشر انتقال، Prefetch عند الاقتراب من الرابط، Cache قصير مرتبط بالمستخدم، ودمج للطلبات المتزامنة.
- أضيفت مهلات زمنية لطلبات Supabase ومزودي الذكاء الاصطناعي حتى لا تبقى الواجهة معلقة.
- أضيف `006_performance_config_snapshot.sql` لدمج بيانات الداشبورد والإعدادات والمستخدم في استعلامات RLS-aware قليلة مع Indexes للمسارات الأكثر استخدامًا.
- أصبح الرفع الجماعي يعالج ضغط الصور بعاملين فقط، يرسل ردًا مختصرًا لكل أصل، ثم يحدّث الطابور مرة واحدة.
- تم إصلاح رابط Organization → Locations بحيث يفتح المبنى المطلوب فعلًا.
- رُفع إصدار Service Worker إلى R14 مع تنظيف Cache القديم، وحماية إضافية تمنع ظهور Sidebar قديم داخل الواجهة الجديدة.
- أزيلت بقايا Cloudflare D1/Vite وDrizzle غير المستخدمة؛ النسخة أصبحت ذات مسار تقني واحد واضح: Next.js + Supabase + Vercel.

## خطوة قاعدة البيانات المطلوبة

إذا كانت الملفات حتى `005_dashboard_snapshot.sql` مطبقة بالفعل، شغّل فقط:

```text
supabase/migrations/006_performance_config_snapshot.sql
```

من Supabase Dashboard → SQL Editor، ثم انشر النسخة الجديدة. الملف قابل لإعادة التشغيل بأمان لأنه يستخدم `create or replace` و`create index if not exists`.

## حدود الاختبار الحي

البناء، الأنواع، Lint، اختبارات المصدر، المسارات العامة وحدود المصادقة يمكن فحصها محليًا. أما إنشاء أصل حقيقي، استجابة Gemini/OpenAI، الكاميرا وGPS فتحتاج مفاتيح المشروع الفعلية وبيئة HTTPS، ولذلك يجب تنفيذ فحص قبول أخير بعد النشر.
